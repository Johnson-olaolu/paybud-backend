<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width">
  <title>Order Approved</title>
  <style>
    body { margin:0; padding:24px; background:#f5f7fb; font-family:Arial, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto; color:#111; }
    .box { max-width:600px; margin:0 auto; background:#fff; padding:20px; border-radius:6px; }
    a.cta { display:inline-block; background:#16a34a; color:#fff; padding:10px 14px; border-radius:6px; text-decoration:none; }
    p { margin:12px 0; }
    .muted { color:#6b7280; font-size:13px; }
  </style>
</head>
<body>
  <div class="box">

    <p>Hi {{#if vendorName}}{{vendorName}}{{else}}there{{/if}},</p>

    <p>
      Great news — 
      {{#if clientName}}{{clientName}}{{else}}the client{{/if}}
      has approved your order
      {{#if orderId}}<strong>#{{orderId}}</strong>{{/if}}{{#if approvalDate}} on {{approvalDate}}{{/if}}.
    </p>

    <p>You can now proceed with fulfillment.</p>

    {{#if orderUrl}}
      <p><a href="{{orderUrl}}" class="cta" target="_blank" rel="noopener noreferrer">View order</a></p>
      <p class="muted">Or use this link: {{orderUrl}}</p>
    {{else}}
      <p class="muted">No order link was provided.</p>
    {{/if}}

    <p class="muted">Thanks,<br>{{#if companyName}}{{companyName}}{{else}}The team{{/if}}</p>
  </div>
</body>
</html>
